import React from 'react';
import '_styles/style.css';

const p = {
    width: '100%',
    textAlign: 'center',
    color: 'black',
    fontWeight: 'bold'
};

const Error = () => {
    return <p style={p}>NOT FOUND CUSTOM PAGE</p>;
};

export default Error;
